<div class="container">
  <div class="row text-right" style="margin-top: 15px;">
    <div class="col-xs-10 col-xs-offset-2">
      <h2>My Clean Car</h2>
      <h4 style=" color: #00FF00;">book an eco wash</h4>
    </div>
  </div>

  <div class="row" style="margin-top: 50px;">
    <div class="col-xs-12">
      <h2>Your cleaning will be done by <%= @order.cleaning.user.firstname %></h2>
        <%= form_tag order_payments_path(@order) do %>
          <article>
            <label class="amount">
              <span>Amount: <%= humanized_money_with_symbol(@order.amount) %></span>
            </label>
          </article>
    </div>
  </div>

  <script src="https://checkout.stripe.com/checkout.js" class="stripe-button"
    data-key="<%= Rails.configuration.stripe[:publishable_key] %>"
    data-name="My Cleaning"
    data-email="<%= current_user.email %>"
    data-description="Cleaning <%= @order.cleaning.id %>"
    data-amount="<%= @order.amount_cents %>"
    data-currency="<%= @order.amount.currency %>"></script>

  <% end %>
</div>
