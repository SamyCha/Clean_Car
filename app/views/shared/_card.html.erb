
<div class="container">
  <% @cleaners.each do |cleaner| %>
  <div class="col-xs-12">
    <div class="row">
      <div class="card-user">
        <img src="..." class="card-user-avatar">
        <%= image_tag "profil.png", id: "logo", height: 60 %>
          <div class="card-user-infos">
          <%= cleaner.email %>
          <%#= @cleaners.firstname %>
          <%#= @cleaners.rating %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
